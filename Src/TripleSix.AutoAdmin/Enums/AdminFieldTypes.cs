namespace TripleSix.AutoAdmin.Enums
{
    /// <summary>
    /// phân loại field của admin.
    /// </summary>
    public enum AdminFieldTypes
    {
        // mã định danh
        Id = 1,

        // hình ảnh / video
        Media = 2,

        // HTML editor
        HTMLEditor = 3,
    }
}